<div class="product">
  <div data-section-id="{{ section.id }}" data-section-type="product" data-enable-history-state="true" itemscope itemtype="http://schema.org/Product">

    {%- assign current_variant = product.selected_or_first_available_variant -%}
    {%- assign featured_image = current_variant.featured_image | default: product.featured_image -%}

    <meta itemprop="name" content="{{ product.title }}{% unless product.has_only_default_variant %} - {{ current_variant.title }}{% endunless %}">
    <meta itemprop="url" content="{{ shop.url }}{{ current_variant.url }}">
    <meta itemprop="brand" content="{{ product.vendor }}">
    <meta itemprop="image" content="{{ featured_image | img_url: '1024x1024' }}">
    <meta itemprop="description" content="{{ product.description | strip_html | escape }}">

    <div class="grid wrap">
      <div class="grid__item medium-up--three-eighths">
        <h2 class="product-title">{{ product.title }}</h2>
        <div class="rte">
          {{ product.description }}
        </div>
        <div itemprop="offers" itemscope itemtype="http://schema.org/Offer">
          <meta itemprop="priceCurrency" content="{{ shop.currency }}">
          <meta itemprop="price" content="{{ current_variant.price | divided_by: 100.00 }}">
          <link itemprop="availability" href="http://schema.org/{% if current_variant.available %}InStock{% else %}OutOfStock{% endif %}">

          <form action="/cart/add" method="post" enctype="multipart/form-data">
            {% unless product.has_only_default_variant %}
              {% for option in product.options_with_values %}
                <div class="grid">
                  <div class="grid__item medium-up--three-quarters selector-wrapper js product-size-quantity">
                    <select
                      id="SingleOptionSelector-{{ forloop.index0 }}"
                      data-single-option-selector
                      data-index="option{{ option.position }}">
                      {% for value in option.values %}
                        <option
                          value="{{ value | escape }}"
                          {% if option.selected_value == value %}selected="selected"{% endif %}>
                            {{ value }}
                        </option>
                      {% endfor %}
                    </select>
                  {% endfor %}
                {% endunless %}
                <select name="id" class="no-js" data-product-select>
                  {% for variant in product.variants %}
                    <option
                      {% if variant == current_variant %}selected="selected"{% endif %}
                      {% unless variant.available %}disabled="disabled"{% endunless %}
                      value="{{ variant.id }}">
                        {{ variant.title }}
                    </option>
                  {% endfor %}
                </select>
              </div>
              <div class="grid__item medium-up--one-quarter product-size-quantity">
                <input type="number" class="mod" id="Quantity" name="quantity" value="1" min="1">
              </div>
            </div>
            <div class="data-price-wrapper">
              <div data-price-wrapper>
                <span data-product-price>
                  {{ current_variant.price | money }}
                </span>

                {% if product.compare_at_price_max > product.price %}
                  <span class="visually-hidden" data-compare-text>{{ 'products.product.regular_price' | t }}</span>
                  <s data-compare-price>
                    {% if current_variant.compare_at_price > current_variant.price %}
                      {{ current_variant.compare_at_price | money }}
                    {% endif %}
                  </s>
                {% endif %}
              </div>
            </div>
            <button
              type="submit"
              name="add"
              data-add-to-cart
              {% unless current_variant.available %}disabled="disabled"{% endunless %}>
                <span data-add-to-cart-text>
                  {% if current_variant.available %}
                    {{ 'products.product.add_to_cart' | t }}
                  {% else %}
                    {{ 'products.product.sold_out' | t }}
                  {% endif %}
                </span>
            </button>
          </form>

        </div>

        {% unless product == empty %}
          <script type="application/json" data-product-json>
            {{ product | json }}
          </script>
        {% endunless %}
      </div>
      <div class="grid__item medium-up--five-eighths">
        {% if featured_image != blank %}
          {% if product.images.size == 1 %}
            <img class="product-image-solo" src="{{ featured_image | img_url: '1024x1024' }}" alt="{{ featured_image.alt | escape }}" data-product-featured-image>

          {% else%}
            <div class="grid product-images">
              <div class="grid__item medium-up--three-quarters">
                <img class="product-image-multiple" src="{{ featured_image | img_url: '1024x1024' }}" alt="{{ featured_image.alt | escape }}" data-product-featured-image>
              </div>
              <div class="grid__item medium-up--one-quarter">
                <ul class="product-image-list">
                  {% for image in product.images %}
                    <li data-product-variant="{{ forloop.index0 }}">
                      <a href="{{ image.src | img_url: '1024x1024' }}" data-product-single-thumbnail>
                        <img src="{{ image.src | img_url: '1024x1024' }}" alt="{{ image.alt | escape }}">
                      </a>
                    </li>
                  {% endfor %}
                </ul>
              </div>
            </div>
          {% endif %}
        {% endif %}
      </div>
    </div>
    <div style="background-color: {{ variant.metafields.product_info.color_scent }}" class="product-details">
      <div class="grid wrap">
        <div class="grid__item medium-up--one-third one-whole">
          <h2>ingredients</h2>
          <p>{{ variant.metafields.product_info.ingredients }}</p>
        </div>
        <div class="grid__item medium-up--one-third one-whole">
          <h2>how to</h2>
          <p>{{ product.metafields.product_info.to_use }}</p>
        </div>
        <div class="grid__item medium-up--one-third one-whole">
          <h2>tips</h2>
          <p>{{ product.metafields.product_info.tips }}</p>
        </div>
      </div>
    </div>
  </div>
</div>

{% schema %}
  {
    "name": "Product pages",
    "settings": [
      {
        "type": "checkbox",
        "id": "show_share_buttons",
        "label": "Show social sharing buttons",
        "default": true
      }
    ]
  }
{% endschema %}
